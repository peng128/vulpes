-RelalgProjection(projects=[t2.h, b, CASE(=(a,1),s) as casefunction, CAST(d,VARCHAR) as t])
  -RelalgSelection(predicate=OR(AND(=(a,-(d,1)),AND(>(s,3),<(s,4))),>(g,5)))
    -RelalgJoin(joinType=INNER_JOIN, joinSide=FULL, condition==(t2.id,t3.id))
      -RelalgJoin(joinType=INNER_JOIN, joinSide=FULL, condition==(t1.id,t2.id))
        -RelalgAlias(alias=t1)
          -RelalgScan(tableIdentifier=embedded-catalog.test.table1)
        -RelalgAlias(alias=t2)
          -RelalgScan(tableIdentifier=embedded-catalog.test.table2)
      -RelalgAlias(alias=t3)
        -RelalgScan(tableIdentifier=embedded-catalog.test.table3)